<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <EditTextPreference
        android:key="lat"
        android:defaultValue="50.9"
        android:title="Latitude"
        android:summary="Latitude to show the map at initially." />
    <EditTextPreference
        android:key="lon"
        android:defaultValue="-1.4"
        android:title="Longitude"
        android:summary="Longitude to show the map at initially." />
    <EditTextPreference
        android:key="zoomLv"
        android:defaultValue="14.0"
        android:title="Default Zoom Level"
        android:summary="Change the default zoom level for the app." />
    <ListPreference
        android:key="mapStyle"
        android:title="Choose a map style"
        android:summary="Select a map style to display the map in."
        android:entries="@array/mapSelector"
        android:entryValues="@array/mapSelectorValues" />
</PreferenceScreen>